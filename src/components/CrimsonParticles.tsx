/**
 * Global floating crimson particle overlay.
 * Renders a full-viewport CSS particle field matching the
 * deep crimson bokeh aesthetic from the reference image.
 */
export default function CrimsonParticles() {
  return <div className="crimson-particles" aria-hidden="true" />;
}
